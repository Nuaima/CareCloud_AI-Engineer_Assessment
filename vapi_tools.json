[
  {
    "type": "function",
    "function": {
      "name": "lookup_patient_by_phone",
      "description": "Check whether an active patient already exists for a U.S. phone number. Use after collecting a valid phone number.",
      "parameters": {
        "type": "object",
        "properties": {
          "phone_number": {"type": "string", "description": "10-digit U.S. phone number"}
        },
        "required": ["phone_number"]
      }
    },
    "server": {"url": "https://YOUR-DOMAIN/vapi/tool"}
  },
  {
    "type": "function",
    "function": {
      "name": "save_patient",
      "description": "Persist a new patient only after the caller explicitly confirms the complete read-back.",
      "parameters": {
        "type": "object",
        "properties": {
          "first_name": {"type": "string"},
          "last_name": {"type": "string"},
          "date_of_birth": {"type": "string", "description": "MM/DD/YYYY"},
          "sex": {"type": "string", "enum": ["Male", "Female", "Other", "Decline to Answer"]},
          "phone_number": {"type": "string"},
          "email": {"type": ["string", "null"]},
          "address_line_1": {"type": "string"},
          "address_line_2": {"type": ["string", "null"]},
          "city": {"type": "string"},
          "state": {"type": "string"},
          "zip_code": {"type": "string"},
          "insurance_provider": {"type": ["string", "null"]},
          "insurance_member_id": {"type": ["string", "null"]},
          "preferred_language": {"type": "string"},
          "emergency_contact_name": {"type": ["string", "null"]},
          "emergency_contact_phone": {"type": ["string", "null"]}
        },
        "required": ["first_name", "last_name", "date_of_birth", "sex", "phone_number", "address_line_1", "city", "state", "zip_code"]
      }
    },
    "server": {"url": "https://YOUR-DOMAIN/vapi/tool"}
  },
  {
    "type": "function",
    "function": {
      "name": "update_patient",
      "description": "Update an existing patient after the caller confirms the final corrected information.",
      "parameters": {
        "type": "object",
        "properties": {
          "patient_id": {"type": "string"},
          "first_name": {"type": "string"},
          "last_name": {"type": "string"},
          "date_of_birth": {"type": "string"},
          "sex": {"type": "string"},
          "phone_number": {"type": "string"},
          "email": {"type": ["string", "null"]},
          "address_line_1": {"type": "string"},
          "address_line_2": {"type": ["string", "null"]},
          "city": {"type": "string"},
          "state": {"type": "string"},
          "zip_code": {"type": "string"},
          "insurance_provider": {"type": ["string", "null"]},
          "insurance_member_id": {"type": ["string", "null"]},
          "preferred_language": {"type": "string"},
          "emergency_contact_name": {"type": ["string", "null"]},
          "emergency_contact_phone": {"type": ["string", "null"]}
        },
        "required": ["patient_id"]
      }
    },
    "server": {"url": "https://YOUR-DOMAIN/vapi/tool"}
  }
]
